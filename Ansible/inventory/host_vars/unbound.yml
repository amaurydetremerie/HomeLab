---
# Résolveur DNS récursif — LXC 9087 (10.0.0.87) sur Wasp
# Architecture : AdGuard (10.0.0.86) → Unbound → root hints

hardening_ufw_rules:
  - port: "53"
    proto: udp
    rule: allow
  - port: "53"
    proto: tcp
    rule: allow