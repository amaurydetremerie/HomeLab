---
- name: restart ssh
  ansible.builtin.systemd:
    name: "{{ hardening_ssh_service }}"
    state: restarted

- name: restart fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: restarted