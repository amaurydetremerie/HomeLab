http:
  address: 0.0.0.0:{{ adguard_port }}
  session_ttl: 720h

users:
  - name: {{ vault_adguard_user }}
    password: {{ vault_adguard_password | password_hash('bcrypt') }}

dns:
  bind_hosts:
    - 0.0.0.0
  port: 53