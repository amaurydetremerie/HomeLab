http:
  services:
    gatus-local:
      loadBalancer:
        servers:
          - url: "http://10.100.0.102:8080"

  routers:
    gatus:
      rule: "Host(`gatus.wiserisk.be`)"
      entryPoints:
        - "web"
      middlewares:
        - "security@file"
        - "compression@file"
      service: "gatus-local@file"
    gatus-local:
      rule: "Host(`gatus.wiserisk.home`)"
      entryPoints:
        - "web"
      middlewares:
        - "redirect-to-https@file"
      service: "noop@internal"
    gatus-local-secure-local:
      rule: "Host(`gatus.wiserisk.home`)"
      entryPoints:
        - "websecure"
      middlewares:
        - "security@file"
        - "compression@file"
      service: "gatus-local@file"
      tls: { }