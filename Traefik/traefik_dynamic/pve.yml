http:
  routers:
    pve-local:
      rule: "Host(`flash.pve.wiserisk.home`) || Host(`wasp.pve.wiserisk.home`)"
      entryPoints:
        - "web"
      middlewares:
        - "redirect-to-https@file"
      service: "noop@internal"

tcp:
  services:
    flash-pve-secure-tcp:
      loadBalancer:
        servers:
          - address: "10.0.0.81:8006"
    wasp-pve-secure-tcp:
      loadBalancer:
        servers:
          - address: "10.0.0.82:8006"
  routers:
    flash-pve-secure-local-tcp:
      rule: "HostSNI(`flash.pve.wiserisk.home`)"
      entryPoints:
        - "websecure"
      service: "flash-pve-secure-tcp@file"
      priority: 2
      tls:
        passthrough: true
    wasp-pve-secure-local-tcp:
      rule: "HostSNI(`wasp.pve.wiserisk.home`)"
      entryPoints:
        - "websecure"
      service: "wasp-pve-secure-tcp@file"
      priority: 2
      tls:
        passthrough: true