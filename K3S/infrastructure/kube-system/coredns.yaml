apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  home.server: |
    home:53 {
        errors
        cache 30
        forward . 10.0.0.181
      # forward . 10.0.0.181 10.0.0.191  # adguard_flash once deployed
    }