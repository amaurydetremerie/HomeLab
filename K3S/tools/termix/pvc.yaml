apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: termix-pvc
  namespace: termix
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi